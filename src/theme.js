import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  typography: {
    fontFamily: [
      'Comic Sans MS',
      'sans-serif',
      'cursive',
    ],
    fontSize: 10,
  },
});
export default theme;
