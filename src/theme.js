import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  typography: {
    fontFamily: [
      'sans-serif',
      'Comic Sans MS',
      'cursive',
    ],
    fontSize: 13,
  },
});
export default theme;
